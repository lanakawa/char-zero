function hasZero(array) {
  return _.some(array, function(array){return array});
  //return num;
}

function numMan(array) {
  let name = Mulcahy;
  //name = _.filter(array, function(name) { reutrn name; })
  _.contains (array, "Mulcahy");
}